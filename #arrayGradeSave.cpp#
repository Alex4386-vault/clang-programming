include <stdio.h>

int main() {
  for (int num = 2; num < 100000000 ; num++) {
    bool isPrime = true;
    for (int i = 2; i <= (num / 2); i++) {
      if (num % i == 0) isPrime = false;
       break;
      }
    }
        
    if 