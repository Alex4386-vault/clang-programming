#include <stdio.h>

int main() {
	// This is how to give a FUCK to your 
	// coworkers by using shitty indentations

	int hours;
	scanf("%d", &hours);

	if (hours < 40)
		if (hours < 20)
			printf("you are lazy");
	else
		printf("work harder");
}