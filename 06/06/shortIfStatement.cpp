#include <stdio.h>

int main() {
	// This is how to give a FUCK to your 
	// coworkers by using shitty indentations

	int hours;
	scanf("%d", &hours);

	/*
	if (hours > 40)
		printf("Good");
	else
		printf("bad");
	*/

	//puts((hours > 40) ? "Good" : "Bad");
	//printf((hours > 40) ? "Good" : "Bad");

	// hours > 40 ? puts("Good") : puts("Bad");

	// hours > 40 ? printf("Good") : printf("Bad");
}